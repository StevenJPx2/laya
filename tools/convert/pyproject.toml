[project]
name = "laya-coreml-convert"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "coremltools>=8.3",
  "numpy>=2.0",
  "torch>=2.6",
  "safetensors>=0.6",
  "mlx>=0.32.2; sys_platform == 'darwin' and platform_machine == 'arm64'",
]

[tool.uv]
package = false
